# Keep scripts and CI config LF so they run correctly on the macOS runner,
# regardless of the committer's core.autocrlf setting.
*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
Podfile text eol=lf
